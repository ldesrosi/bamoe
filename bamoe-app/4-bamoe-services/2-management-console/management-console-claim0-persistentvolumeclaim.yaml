apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: management-console-claim0
  name: management-console-claim0
spec:
  storageClassName: ibmc-block-bronze
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
